<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Thiết lập màu trong suốt -->
    <solid android:color="#00FFFFFF" />
    <!-- Thiết lập viền -->
    <stroke
        android:width="1dp"
        android:color="#EC2578" />
    <!-- Các thuộc tính còn lại -->
</shape>
